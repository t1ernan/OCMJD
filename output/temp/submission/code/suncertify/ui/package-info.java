/**
 * This package makes up the the presentation layer of the application. It's responsible for
 * providing the user with a GUI interface so they can configure and use the application. It also
 * contains the {@link CliLauncher} class, which contains the main method enables the application to
 * be launched from the command line.
 */
package suncertify.ui;